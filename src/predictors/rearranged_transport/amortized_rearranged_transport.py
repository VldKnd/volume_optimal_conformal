from __future__ import annotations

import torch

from configs.predictors.rearranged_transport.amortized_rearranged_transport import (
    AmortizedRearrangedTransportPredictorConfig,
)
from predictors.rearranged_transport.rearranged_transport import (
    RearrangedTransportPredictor,
)
from predictors.transport.base import BaseTransportPredictor


class AmortizedRearrangedTransport(RearrangedTransportPredictor):
    """Coverage-conditioned family of Gaussian-preserving rearrangements.

    For each fixed coverage mass ``alpha``, the rearrangement is generated by a
    Gaussian-preserving vector field conditioned on ``[x, alpha]``. The wrapped
    transport predictor continues to receive only the original covariates ``x``.

    Every rearrangement, transport, and score call requires the intended
    coverage mass explicitly. Conditioning alone does not impose nesting of the
    learned regions as ``alpha`` varies.
    """

    config_class = AmortizedRearrangedTransportPredictorConfig

    def __init__(
        self,
        config: AmortizedRearrangedTransportPredictorConfig,
        transport_predictor: BaseTransportPredictor,
    ):
        if not isinstance(config, AmortizedRearrangedTransportPredictorConfig):
            raise TypeError(
                "config must be an "
                "AmortizedRearrangedTransportPredictorConfig instance."
            )

        super().__init__(
            config=config,
            transport_predictor=transport_predictor,
        )

    def _rearrangement_context_dimension(self) -> int:
        return self.x_dim + 1

    def _prepare_rearrangement_context(
        self,
        x: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        x = self.to_device(x)

        if x.ndim == 0 or x.shape[-1] != self.x_dim:
            actual_dimension = None if x.ndim == 0 else x.shape[-1]
            raise ValueError(
                f"Expected x.shape[-1] = {self.x_dim}, got {actual_dimension}."
            )

        alpha_tensor = torch.as_tensor(
            alpha,
            device=x.device,
            dtype=x.dtype,
        )
        batch_shape = x.shape[:-1]

        if alpha_tensor.ndim == 0:
            alpha_tensor = alpha_tensor.expand(*batch_shape, 1)
        else:
            if alpha_tensor.shape == batch_shape:
                alpha_tensor = alpha_tensor.unsqueeze(-1)

            try:
                alpha_tensor = torch.broadcast_to(
                    alpha_tensor,
                    (*batch_shape, 1),
                )
            except RuntimeError as error:
                raise ValueError(
                    "alpha must be scalar or broadcastable to "
                    f"x.shape[:-1] + (1,), got alpha.shape="
                    f"{tuple(alpha_tensor.shape)} and x.shape={tuple(x.shape)}."
                ) from error

        if not torch.isfinite(alpha_tensor).all():
            raise ValueError("alpha must contain only finite values.")

        if not ((alpha_tensor > 0.0) & (alpha_tensor < 1.0)).all():
            raise ValueError("alpha must lie strictly between 0 and 1.")

        return torch.cat([x, alpha_tensor], dim=-1)

    def rearrangement_pushforward(
        self,
        x: torch.Tensor,
        u: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        u = self.to_device(u)
        context = self._prepare_rearrangement_context(x=x, alpha=alpha)
        return self.rearrangement_flow.forward(u=u, x=context)

    def rearrangement_pullback(
        self,
        x: torch.Tensor,
        u: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        u = self.to_device(u)
        context = self._prepare_rearrangement_context(x=x, alpha=alpha)
        return self.rearrangement_flow.inverse(
            u=u,
            x=context,
            start_time=1.0,
        )

    def pushforward(
        self,
        x: torch.Tensor,
        u: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        rearranged_u = self.rearrangement_pushforward(
            x=x,
            u=u,
            alpha=alpha,
        )
        return self.transport_pushforward(x=x, u=rearranged_u)

    def pullback(
        self,
        x: torch.Tensor,
        y: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        transport_u = self.transport_pullback(x=x, y=y)
        return self.rearrangement_pullback(
            x=x,
            u=transport_u,
            alpha=alpha,
        )

    def log_det(
        self,
        x: torch.Tensor,
        u: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        """Return ``log |det D_u (T_x o S_{x, alpha})(u)|``."""
        rearranged_u = self.rearrangement_pushforward(
            x=x,
            u=u,
            alpha=alpha,
        )
        return self.transport_log_det(
            x=x,
            u=rearranged_u,
        ) + self._rearrangement_log_det_from_pushforward(
            u=self.to_device(u),
            rearranged_u=rearranged_u,
        )

    def rearrangement_log_det(
        self,
        x: torch.Tensor,
        u: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        rearranged_u = self.rearrangement_pushforward(
            x=x,
            u=u,
            alpha=alpha,
        )
        return self._rearrangement_log_det_from_pushforward(
            u=self.to_device(u),
            rearranged_u=rearranged_u,
        )

    def multivariate_score(
        self,
        x: torch.Tensor,
        y: torch.Tensor,
        alpha: torch.Tensor | float,
    ) -> torch.Tensor:
        return self.pullback(x=x, y=y, alpha=alpha)


AmortizedRearrangedTransportPredictor = AmortizedRearrangedTransport
